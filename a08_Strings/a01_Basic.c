#include <stdio.h>

int main (){
    // char ch = '\0';
    // int x = 0;
    // char a = (char)x;
    // printf("%c",x);

    // char str[] = {'h','e','l','l','o','\0'}; //OR char str[] = {"hello"};
    char str[] = {"hello"};
    int i = 0;
    // while(str[i]!='\0'){
    //     printf("%c",str[i]);
    //     i++;
    // } //ORR
    printf("%s\n",str); //ORR
    puts(str);

    
    
    return 0;
}